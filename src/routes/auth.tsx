import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { useState } from "react";
import { ArrowLeft, ArrowRight, Check, LockKeyhole } from "lucide-react";
import { supabase, isSupabaseConfigured } from "@/integrations/supabase/client";

export const Route = createFileRoute("/auth")({ component: AuthPage });
function AuthPage() {
  const navigate = useNavigate(); const [mode, setMode] = useState<"login" | "signup">("login"); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [name, setName] = useState(""); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false);
  async function submit(e: React.FormEvent) { e.preventDefault(); setBusy(true); setMessage(""); if (!isSupabaseConfigured) { setMessage("Connect Supabase to enable sign-in."); setBusy(false); return; } const result = mode === "login" ? await supabase.auth.signInWithPassword({ email, password }) : await supabase.auth.signUp({ email, password, options: { data: { full_name: name }, emailRedirectTo: window.location.origin + "/" } }); if (result.error) setMessage(result.error.message); else { setMessage(mode === "signup" && !result.data.session ? "Check your email to confirm your account." : "Signed in."); navigate({ to: "/" }); } setBusy(false); }
  return <main className="grid min-h-screen lg:grid-cols-[.85fr_1.15fr]"><section className="hidden bg-[#0d1b35] p-10 text-white lg:flex lg:flex-col"><Link to="/" className="flex items-center gap-2 text-lg font-bold"><span className="grid size-8 place-items-center rounded-lg bg-blue-500">C</span> ClientDesk</Link><div className="mt-auto max-w-md"><p className="mb-5 text-sm font-semibold uppercase tracking-[.2em] text-blue-300">One calm place to do great work</p><h1 className="text-5xl font-bold leading-[1.08] tracking-tight">Projects your clients can feel good about.</h1><ul className="mt-10 space-y-4 text-sm text-slate-300"><li className="flex gap-3"><Check className="text-blue-300" size={18}/> Share progress without status meetings</li><li className="flex gap-3"><Check className="text-blue-300" size={18}/> Keep feedback and files in one place</li><li className="flex gap-3"><Check className="text-blue-300" size={18}/> Give every client a polished private portal</li></ul></div><p className="mt-12 text-xs text-slate-500">© 2025 ClientDesk</p></section><section className="flex items-center justify-center bg-background px-6 py-12"><div className="w-full max-w-md"><Link to="/" className="mb-12 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-foreground"><ArrowLeft size={16}/> Back to home</Link><div className="mb-8"><div className="mb-5 grid size-11 place-items-center rounded-xl bg-blue-100 text-primary"><LockKeyhole size={21}/></div><h2 className="text-3xl font-bold tracking-tight">{mode === "login" ? "Welcome back" : "Create your workspace"}</h2><p className="mt-2 text-sm text-muted-foreground">{mode === "login" ? "Sign in to manage your client work." : "Start building clearer client relationships."}</p></div><form onSubmit={submit} className="space-y-4">{mode === "signup" && <label className="block text-sm font-semibold">Your name<input required value={name} onChange={e => setName(e.target.value)} className="mt-2 h-12 w-full rounded-lg border border-input bg-white px-3 text-sm outline-none focus:border-primary" placeholder="Jordan Diaz" /></label>}<label className="block text-sm font-semibold">Email<input required type="email" value={email} onChange={e => setEmail(e.target.value)} className="mt-2 h-12 w-full rounded-lg border border-input bg-white px-3 text-sm outline-none focus:border-primary" placeholder="you@studio.com" /></label><label className="block text-sm font-semibold">Password<input required minLength={6} type="password" value={password} onChange={e => setPassword(e.target.value)} className="mt-2 h-12 w-full rounded-lg border border-input bg-white px-3 text-sm outline-none focus:border-primary" placeholder="At least 6 characters" /></label>{message && <p role="status" className="rounded-lg bg-blue-50 px-3 py-2 text-sm text-blue-800">{message}</p>}<button disabled={busy} className="flex h-12 w-full items-center justify-center gap-2 rounded-lg bg-primary text-sm font-bold text-white transition hover:brightness-110 disabled:opacity-60">{busy ? "Working…" : mode === "login" ? "Sign in" : "Create account"}<ArrowRight size={17}/></button></form><p className="mt-7 text-center text-sm text-muted-foreground">{mode === "login" ? "New to ClientDesk?" : "Already have an account?"} <button onClick={() => { setMode(mode === "login" ? "signup" : "login"); setMessage(""); }} className="font-bold text-primary hover:underline">{mode === "login" ? "Create an account" : "Sign in"}</button></p></div></section></main>;
}
