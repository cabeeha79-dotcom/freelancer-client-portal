import { Link } from "@tanstack/react-router";
import { ArrowUpRight, MoreHorizontal, UserRound } from "lucide-react";
import type { Project } from "@/lib/data";

const statusStyles: Record<Project["status"], string> = { "Not Started": "bg-slate-100 text-slate-700", "In Progress": "bg-blue-50 text-blue-700", Review: "bg-amber-50 text-amber-700", Completed: "bg-emerald-50 text-emerald-700" };
export function ProjectCard({ project }: { project: Project }) {
  return <article className="group rounded-2xl border border-border bg-white p-5 shadow-[0_8px_30px_-24px_rgba(17,35,65,.35)] transition hover:-translate-y-0.5 hover:border-blue-200 hover:shadow-lg"><div className="flex items-start justify-between gap-4"><div className="flex min-w-0 items-center gap-3"><div className="grid size-11 shrink-0 place-items-center rounded-xl bg-blue-50 font-bold text-blue-700">{project.name.slice(0, 1)}</div><div className="min-w-0"><Link to="/projects/$projectId" params={{ projectId: project.id }} className="line-clamp-2 min-h-10 text-base font-bold leading-5 hover:text-primary">{project.name}</Link><div className="mt-1 flex items-center gap-1.5 text-xs text-muted-foreground"><UserRound size={13}/>{project.clientName}</div></div></div><button aria-label={`More options for ${project.name}`} className="rounded-md p-1.5 text-muted-foreground hover:bg-muted"><MoreHorizontal size={18}/></button></div><div className="mt-6 flex items-center justify-between text-xs"><span className={`rounded-full px-2.5 py-1 font-semibold ${statusStyles[project.status]}`}>{project.status}</span><span className="font-medium text-muted-foreground">Updated {project.updated}</span></div><div className="mt-4"><div className="mb-2 flex justify-between text-xs font-semibold"><span className="text-muted-foreground">Progress</span><span>{project.progress}%</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-primary transition-all" style={{ width: `${project.progress}%` }}/></div></div><Link to="/projects/$projectId" params={{ projectId: project.id }} className="mt-5 flex items-center justify-between border-t border-border pt-4 text-sm font-semibold text-primary">Open project <ArrowUpRight size={16} className="transition group-hover:translate-x-0.5 group-hover:-translate-y-0.5"/></Link></article>;
}
