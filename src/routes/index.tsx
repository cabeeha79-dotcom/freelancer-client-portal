import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { Bell, Menu, Plus, Search, ArrowUpRight, X } from "lucide-react";
import { AppSidebar } from "@/components/clientdesk/AppSidebar";
import { ProjectCard } from "@/components/clientdesk/ProjectCard";
import { loadProjects, saveProjects, type Project } from "@/lib/data";

export const Route = createFileRoute("/")({ head: () => ({ meta: [{ title: "Overview — ClientDesk" }, { name: "description", content: "Your ClientDesk project overview." }] }), component: Dashboard });
function Dashboard() {
  const [projects, setProjects] = useState<Project[]>(loadProjects); const [showCreate, setShowCreate] = useState(false); const [mobileNav, setMobileNav] = useState(false); const [name, setName] = useState(""); const [client, setClient] = useState(""); const [email, setEmail] = useState(""); const [description, setDescription] = useState("");
  function createProject(e: React.FormEvent) { e.preventDefault(); const project: Project = { id: `p${Date.now()}`, name, clientId: `client-${Date.now()}`, clientName: client, clientEmail: email, description, status: "Not Started", progress: 0, updated: "Just now", token: `${name.toLowerCase().replace(/[^a-z0-9]+/g, "-")}-${Math.random().toString(36).slice(2, 6)}`, files: [], messages: [] }; const next = [project, ...projects]; setProjects(next); saveProjects(next); setShowCreate(false); setName(""); setClient(""); setEmail(""); setDescription(""); }
  return <div className="flex min-h-screen bg-background"><AppSidebar onCreate={() => setShowCreate(true)}/>{mobileNav && <div className="fixed inset-0 z-40 bg-[#0d1b35]/40 lg:hidden" onClick={() => setMobileNav(false)}><div className="h-full w-72 bg-white p-5" onClick={e => e.stopPropagation()}><div className="flex items-center justify-between"><b className="text-lg">ClientDesk</b><button aria-label="Close navigation" onClick={() => setMobileNav(false)}><X size={20}/></button></div><button onClick={() => { setShowCreate(true); setMobileNav(false); }} className="mt-8 flex w-full items-center justify-center gap-2 rounded-lg bg-primary py-3 text-sm font-semibold text-white"><Plus size={17}/> New project</button><Link to="/" className="mt-6 block rounded-lg bg-accent px-3 py-3 text-sm font-semibold text-accent-foreground">Overview</Link><Link to="/projects" className="mt-1 block rounded-lg px-3 py-3 text-sm font-semibold text-muted-foreground">Projects</Link></div></div>}<main className="min-w-0 flex-1"><header className="flex h-20 items-center justify-between border-b border-border bg-white px-5 sm:px-8"><div className="flex items-center gap-3"><button aria-label="Open navigation" onClick={() => setMobileNav(true)} className="rounded-lg p-2 text-muted-foreground hover:bg-muted lg:hidden"><Menu size={20}/></button><div><p className="text-sm text-muted-foreground">Tuesday, March 25, 2025</p><h1 className="text-xl font-bold tracking-tight sm:text-2xl">Good morning, Jordan</h1></div></div><div className="flex items-center gap-2"><button aria-label="Search projects" className="hidden rounded-lg p-2.5 text-muted-foreground hover:bg-muted sm:block"><Search size={19}/></button><button aria-label="Notifications" className="rounded-lg p-2.5 text-muted-foreground hover:bg-muted"><Bell size={19}/></button><Link to="/auth" className="ml-2 hidden rounded-lg border border-border px-3 py-2 text-sm font-semibold hover:bg-muted sm:block">Account</Link></div></header><div className="mx-auto max-w-7xl p-5 sm:p-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-primary">Workspace overview</p><h2 className="mt-1 text-3xl font-bold tracking-tight">Your projects</h2><p className="mt-2 text-sm text-muted-foreground">Keep every client in the loop, without adding another meeting.</p></div><button onClick={() => setShowCreate(true)} className="inline-flex h-11 items-center justify-center gap-2 rounded-lg bg-primary px-4 text-sm font-bold text-white shadow-sm hover:brightness-110"><Plus size={17}/> New project</button></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-white p-5"><p className="text-sm text-muted-foreground">Active projects</p><p className="mt-2 text-3xl font-bold">{projects.filter(p => p.status !== "Completed").length}</p><p className="mt-2 text-xs font-semibold text-blue-700">Across your workspace</p></div><div className="rounded-2xl border border-border bg-white p-5"><p className="text-sm text-muted-foreground">Awaiting review</p><p className="mt-2 text-3xl font-bold">{projects.filter(p => p.status === "Review").length}</p><p className="mt-2 text-xs font-semibold text-amber-700">Ready for client feedback</p></div><div className="rounded-2xl border border-border bg-white p-5"><p className="text-sm text-muted-foreground">Average progress</p><p className="mt-2 text-3xl font-bold">{Math.round(projects.reduce((a, p) => a + p.progress, 0) / Math.max(projects.length, 1))}%</p><p className="mt-2 text-xs font-semibold text-emerald-700">Keep shipping great work</p></div></div><div className="mt-10 flex items-center justify-between"><h3 className="text-lg font-bold">All projects <span className="ml-1 text-sm font-medium text-muted-foreground">{projects.length}</span></h3><Link to="/projects" className="flex items-center gap-1 text-sm font-bold text-primary hover:underline">View all <ArrowUpRight size={15}/></Link></div><div className="mt-4 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{projects.slice(0, 3).map(project => <ProjectCard key={project.id} project={project}/>)}</div></div></main>{showCreate && <div className="fixed inset-0 z-50 grid place-items-center bg-[#0d1b35]/45 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><h2 className="text-xl font-bold">Create a project</h2><p className="mt-1 text-sm text-muted-foreground">Set up the essentials. You can add files and progress later.</p></div><button aria-label="Close create project" onClick={() => setShowCreate(false)} className="rounded-lg p-2 hover:bg-muted"><X size={18}/></button></div><form onSubmit={createProject} className="mt-6 space-y-4"><label className="block text-sm font-semibold">Project name<input required value={name} onChange={e => setName(e.target.value)} className="mt-2 h-11 w-full rounded-lg border border-input px-3 text-sm" placeholder="e.g. Spring campaign"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm font-semibold">Client name<input required value={client} onChange={e => setClient(e.target.value)} className="mt-2 h-11 w-full rounded-lg border border-input px-3 text-sm" placeholder="Maya Chen"/></label><label className="block text-sm font-semibold">Client email<input required type="email" value={email} onChange={e => setEmail(e.target.value)} className="mt-2 h-11 w-full rounded-lg border border-input px-3 text-sm" placeholder="maya@studio.com"/></label></div><label className="block text-sm font-semibold">Description<textarea required value={description} onChange={e => setDescription(e.target.value)} className="mt-2 min-h-24 w-full resize-y rounded-lg border border-input p-3 text-sm" placeholder="What are you helping them achieve?"/></label><button className="h-11 w-full rounded-lg bg-primary text-sm font-bold text-white hover:brightness-110">Create project</button></form></div></div>}</div>;
}
